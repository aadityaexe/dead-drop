import React from 'react';

export default function Hero() {
  return (
    <section className="hero">
      <h1>Unrecoverable secrets.</h1>
      <p>A zero-knowledge protocol. Your message is encrypted locally. The key never leaves your device. Once read, the data is cryptographically annihilated.</p>
    </section>
  );
}
